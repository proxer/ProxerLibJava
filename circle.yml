machine:
  java:
    version: openjdk7

dependencies:
  override:
    - ./gradlew dependencies --console=plain

test:
  override:
    - ./gradlew build --console=plain
  post:
    - mkdir -p $CIRCLE_TEST_REPORTS/junit/
    - cp -r library/build/test-results/.*xml $CIRCLE_TEST_REPORTS/junit/

